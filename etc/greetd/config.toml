[terminal]
vt = 1

[default_session]
command = '''
tuigreet \
  --cmd sway \
  --power-reboot 'systemctl reboot' \
  --power-shutdown 'systemctl poweroff -i' \
  --remember \
  --time \
  --user-menu \
  --width 72
'''
user = "greeter"
